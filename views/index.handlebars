

<div class="jumbotron mt-4">
    <h1 class="display-4">Bem-vindo ao Blog do NodeJS</h1>
    <P class="lead">Essa aplicação foi desenvolvida com o Node.js</P>
    <hr class="my-4">
    <p>Faça seu Cadastro</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
    </p>
</div>

<hr>

<h2>Postagens Recentes:</h2>


{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success mt-2">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small><br>
        <small>Data de publicação: {{data}}</small>
        <hr>
    </div>
</div>
{{else}}
    <h5>Nenhuma Postagem</h5>
{{/each}}